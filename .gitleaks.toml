[extend]
useDefault = true

[[allowlists]]
description = "Archived URP principle sentence misclassified as a generic API key"
condition = "AND"
targetRules = ["generic-api-key"]
regexTarget = "match"
regexes = ['''APIs first: S3-compatible''']
paths = ['''^archive/''']
