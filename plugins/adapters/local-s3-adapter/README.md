# local-s3-adapter

Reference adapter package for the local S3-compatible gateway. It documents the
Put/Get/Head/List/Delete/range, metadata, tags, and multipart capability shape
without requiring cloud credentials.
