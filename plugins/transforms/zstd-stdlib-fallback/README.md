# zstd-stdlib-fallback

Reference transform package for the exact-byte compression plugin interface. The
local runtime uses the stdlib fallback when native zstd is unavailable and keeps
rehydration exact.
