# Test Results

Command:

```bash
PYTHONPATH=src/python python -m unittest discover -s tests -v
```

Result:

```text
Ran 9 tests
OK
```

Note: the execution environment printed an unrelated spreadsheet-runtime warmup warning before the unittest output. The test process returned exit code 0.
